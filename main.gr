module Hello

from "./expression.gr" include Tree
from "./lexing.gr" include Lexing
from "set" include Set

print("start lexing");
let tokens = Lexing.lex("1 + 2 * 3")

Set.forEach(token => {
    print(token)
}, tokens)
